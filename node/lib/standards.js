//** Standards cli helper functions, for ease of creation and statistics **/
import os

export default class Standard {
  constructor(text) {
    this.rawText = text;
    this.parse(text);
  }

  parse() {

  }
}
